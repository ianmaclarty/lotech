-- App configuration

-- Short unique name used for save file.
lt.config.short_name = "meteor"

-- Screen size for which images have been designed.
-- Images will be scaled to have the same proportions they would
-- have on this screen size.
lt.config.design_width = 960
lt.config.design_height = 640

-- World space dimensions.
lt.config.world_bottom = -5
lt.config.world_top = 5
lt.config.world_left = -7.5
lt.config.world_right = 7.5

-- Script to run first (the .lua suffix should be omitted).
lt.config.start_script = "meteor"
